interface:
  display_name: "Codex Web User Context"
  short_description: "Fetch the current Codex Web user and project on demand."
  default_prompt: "Use $codex-web-user-context to fetch the current Codex Web user and project context."
