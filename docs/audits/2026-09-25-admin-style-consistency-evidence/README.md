# 管理控制台视觉一致性修正

以现有 session list 为唯一参考：主题背景、独立面板卡片、12px 内边距/圆角、8px 列表间距、13px/500 标题与12px元信息。管理页标题保持14px，编辑输入保持16px避免手机聚焦缩放，操作触达保持44px。管理列表不再套一层大白面板或使用分隔线行。系统信息、编辑器、确认框使用相同层级；checkbox 使用主题强调色。

此前管理页的大标题、整块集合面板和较粗行标题偏离已有产品视觉。这轮只重写 admin-ui.css，未改 session list、事件、状态或业务代码。最终源码冻结：2026-09-25T00:30:22Z。

`capture.spec.js` 是本轮取证脚本，未加入常规测试目录。独立运行：

```sh
npx playwright test --config docs/audits/2026-09-25-admin-style-consistency-evidence/playwright.config.js --workers=1
```

主题 fresh-light、retro、dark-gold、oled-black、terminal × 390px/1440px 视口；每组合包含同视口 session list 参照、五个管理页面、用户编辑器和确认框。导航截图等待过渡动画结束。`*-computed.json` 仅作测量记录，不把 CSS token 等值作为长期行为断言。无主题前缀 after 图片是首轮草图，最终审阅使用含主题前缀的文件。

回归截图存于 regression/；原 admin-redesign-evidence 截图已从本轮修改前归档恢复。现有 admin-redesign 用例涵盖5种视口、编辑草稿/保存按钮、确认取消/焦点/单次提交、身份失效、无P95样本，以及320px五主题双语言编辑表单。

受影响优化：OPT-17（样式/响应式）、OPT-18（首屏负载）。保留并由现有测试覆盖相关行为：OPT-09/10（审计滚动/分页）、OPT-06（草稿）、OPT-13/21（身份）、OPT-15（PWA资源）。本轮取证不等于全部弱网场景重验；预算、公共资源和其他管理回归由根代理集成复核。

自评：管理功能本身信息更多，用户卡片需要详情及操作行，因此比普通会话卡片更高；这属于内容差异。列表表面、文字尺度、主题颜色与间距已一致。系统宽表仍保留局部横向滚动以完整呈现指标，不压缩或隐藏数据。

最终源码验证：独立取证10/10通过；admin-redesign 13通过、7按视口条件跳过。CSS原文8609→7748 bytes；原文gzip1872→1748 bytes（非最终构建预算，构建结果由根代理复核）。

## 集成复核

产品代码仅修改 `admin-ui.css`；另将 `public_ui.test.ts` 中项目双列的断言从“必须重复写在手机 media 内”校准为“所有视口共用的基础规则”。未改变列结构、换行、滚动、身份、草稿或确认断言。

根代理管理回归21通过、39按既有视口范围跳过；子代理管理重设计回归13通过、7跳过。472项相关单测初轮471通过，唯一失败为上述旧规则位置断言，修正后该用例通过，无未解决失败。类型检查、lint、构建与真实 HTTP/浏览器构建冒烟均通过。六个新增资源的 MIME、压缩、版本缓存和 ETag 304 校验通过；浏览器错误为0。

首屏 gzip 143035 / 143360 bytes，较此前减少133 bytes；app.js 保持498694 bytes，预算未提高。会话独立回退补丁 reverse --check 通过，未在工作区应用。先前审计图片已按本轮修改前快照逐字节恢复，新回归图片另存。

根代理查看了修改前后同视口列表和管理页，以及五主题最终图；修正了系统设备行偏大的字体和未跟随主题的 checkbox。验证明细见 [verification.json](verification.json)，预算见 [startup-budget.json](startup-budget.json)。最终生产重载状态以 verification.json 中外部 statusFile 为准，排队不等于已经上线。

局限：使用 Chromium 触控/视口模拟，未做真机 Safari、VoiceOver 或真实生产账户写操作。此次样式修正并未重新运行全部21项优化的全部场景。

生产激活于2026-09-25 08:37（北京时间）排队，独立 systemd unit 为 `codex-web-admin-style-activate-20260925t003614.service`。记录时仍为 `waiting_for_idle`、未请求重启；等待本轮及已加载任务空闲后执行一次重载，随后验证本地/公网资源与鉴权边界。期望新版本 `d4e5bacffae6d671461d`。最终 RPC 与重启之间没有原子入站闸门，仍存在很小的并发新任务竞态；此记录不承诺零中断。

生产重载已于2026-09-25 08:38:13（北京时间）完成，外部 runner 状态为 `passed`。后续复核服务 active/running、本地与公网首页和版本均为200且对应新版本，实际 admin-ui.css 正文与本轮审阅源码逐字节一致。
